{
  "name": "youthhack-kc-connect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../api && npm install",
    "vercel-build": "npm run build",
    "start": "cd client && npm run dev"
  },
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "client",
    "api"
  ],
  "devDependencies": {
    "@vitejs/plugin-react-swc": "^4.1.0",
    "vite": "^7.1.10"
  }
}
